import LoginForm from "@/app/login/LoginForm";

const Home = () => {

    return (
        <main>
            <div className={'container'}>
                <LoginForm />
            </div>
        </main>
    );
}

export default Home;
